// import type { SupabaseClient } from "@hyperinkstudio/db";
// import { TABLE_PROFILE_TAGGING_OPTS as TABLE } from "../consts";

// export async function getProfileTaggingOpts(browserClient: SupabaseClient) {
//   const { data, error } = await browserClient.from(TABLE).select("*");

//   return { data, error };
// }
