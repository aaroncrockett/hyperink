import { getTattooImages } from "@hyperinkstudio/api";
import { createSSClient } from "@/business/server";
export async function getImages() {
  // const authClient = await createSSClient();
  // const images = await getTattooImagesContaining(
  //   authClient,
  //   "portfolio-tattoo",
  //   24,
  // );

  // return images;
  return [];
}
